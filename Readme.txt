OpenCV – pip install opencv-python (Face and eye detection).
Imutils – pip install imutils (To get landmarks of eye).
Scipy – pip install scipy (To calculate distance between eye landmarks).
Pygame – pip install pygame (To play alarm sound).
Dlib - pip install dlib ( must have pip upgraded and python of version 3.12 and below) 
https://github.com/z-mahmud22/Dlib_Windows_Python3.x
pip install mediapipe 


pip install dlib pygame opencv-python imutils scipy